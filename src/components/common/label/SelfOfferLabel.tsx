import styles from './selfOfferLabel.module.scss';

const SelfOfferLabel = () => {
  return (
    <div className={styles.label}>
      <div className={styles.text}>자체 공모</div>
    </div>
  );
};

export default SelfOfferLabel;
